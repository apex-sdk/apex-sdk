{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "apex-sdk-docs",
  "version": "1.0.0",
  "description": "Apex SDK Documentation",
  "scripts": {
    "build": "node scripts/build-cloudflare.js",
    "build:simple": "mkdir -p dist && cp -r docs/* dist/",
    "deploy": "npm run build"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260131.0",
    "@types/react": "^19.2.13",
    "@types/react-dom": "^19.2.3"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
