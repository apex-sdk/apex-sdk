{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "apex-sdk-docs",
  "version": "1.0.0",
  "description": "Apex SDK Documentation",
  "scripts": {
    "build": "node scripts/build-cloudflare.js --out out",
    "build:simple": "mkdir -p out && cp -r docs/* out/",
    "deploy": "npm run build",
    "validate-content": "cd apex-sdk-web && npm run validate-content"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260131.0",
    "@types/react": "^19.2.13",
    "@types/react-dom": "^19.2.3"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
