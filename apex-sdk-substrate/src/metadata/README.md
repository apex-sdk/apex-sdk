# Substrate Metadata

This directory contains the generated metadata for various Substrate chains.
These are used for type-safe interaction with the chains.

## Usage

```toml
[dependencies]
apex-sdk-substrate = { version = "0.1.6", features = ["typed-westend"] }
```
